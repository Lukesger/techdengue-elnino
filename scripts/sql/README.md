# SQL auxiliar (TechDengue primary)

Scripts espelhados do backend completo, úteis quando o monorepo roda contra `techdengue_primary` (não no modo demo sem DB).

- `076-elnino-read-all-perfis.sql` — concede `analytics:elnino:read` a todos os perfis ativos.
